@using System.Timers
@page "/timer"

<div class="container">
    <h3>ラップタイマー</h3>

    <div class="container-fluid">
        <section>
            <div class="row">
                <div class="col-sm-6">
                    <h1 class="display-1">@TimerClock.ToString(@"hh\:mm\:ss\.ff")</h1>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    <button type="button" class="btn btn-primary" @onclick="StartTimer">測定開始</button>
                    <button type="button" class="btn btn-danger" @onclick="StopTimer">停止</button>
                    <button type="button" class="btn btn-secondary" @onclick="ResetTimer">リセット</button>
                </div>
            </div>
        </section>

        <br>

        <section>
            <table class="table table-striped table-sm">
                <thead>
                    <tr>
                        <th>周回</th>
                        <th>時間（秒）</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>

                    </tr>
                </tbody>
            </table>
        </section>
    </div>
</div>

@code {
    private static TimeSpan TimerClock = new(0, 0, 0, 0, 0); // 表示用

    private static TimeSpan AddTimeSpan = new(0, 0, 0, 0, 10); // 加算用

    private System.Timers.Timer _timer = new System.Timers.Timer(1); // 1ms間隔

    protected override void OnInitialized()
    {
        _timer.Elapsed += (sender, e) =>
        {
            TimerClock = TimerClock.Add(AddTimeSpan);
            RefreshView();
        };
        _timer.AutoReset = true;

        base.OnInitialized();
    }

    private void StartTimer()
    {
        _timer.Start();
    }

    private void StopTimer()
    {
        _timer.Stop();
        RefreshView();
    }

    private void ResetTimer()
    {
        _timer.Stop();
        TimerClock = new TimeSpan(0, 0, 0, 0, 0);
        RefreshView();
    }

    private void RefreshView()
    {
        InvokeAsync(() => StateHasChanged());
    }
}
